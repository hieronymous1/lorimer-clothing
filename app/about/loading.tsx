import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Studio note loading" title="Preparing the Lorimer studio register." blocks={2} />;
}
